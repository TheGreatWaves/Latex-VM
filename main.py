from src.graph_session import GraphSession

if __name__ == "__main__":
    gs = GraphSession.new()
    # gs.execute("alex = 5")
    # gs.execute("squared(x) = x^2")
    # gs.execute("squared(alex)")
    # gs.execute(r"half(number) = \frac{number}{2}")
    # gs.execute(r"half(alex)")
    # gs.execute("x = 2")
    # gs.execute("f(x) = 2*x")
    # gs.execute("f(3)")
    # gs.execute("double(x) = x*2")
    # gs.execute("double(double(double(1)))")
    # gs.execute(
    #     "quadriple(the_number_to_quadriple) = (double(double(the_number_to_quadriple)))"
    # )

    # gs.execute("v = quadriple(4)")
    # gs.execute("v")
